services:
  - type: web
    name: bis-mitra-ai-assistant
    env: node
    buildCommand: npm install && cd backend && npm install && npm run build:ui
    startCommand: node backend/server.js
    envVars:
      - key: PORT
        value: 10000
